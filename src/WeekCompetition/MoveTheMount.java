package WeekCompetition;
// 周赛 416
// 移山的最少时间
// 动态规划的思想
        // 对于mount高的山 dp[mount-1]表示移走mount高的山的最短时长，dp[0]代表移走高度为1的山需要的最短时长
        // 状态转换方程 :dp[j]=Math.min(dp[j-1]+workTimes[i]*1,
public class MoveTheMount {
    public long minNumberOfSeconds(int mountainHeight, int[] workerTimes) {
        return 0;
    }
}
